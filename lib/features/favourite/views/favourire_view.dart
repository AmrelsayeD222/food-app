import 'package:flutter/material.dart';

class FavourireView extends StatelessWidget {
  const FavourireView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Favourite View"),
      ),
    );
  }
}
